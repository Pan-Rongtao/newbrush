#include "newbrush/gui/controls/Slider.h"

using namespace nb;

Size Slider::measureOverride(const Size & availableSize)
{
	return Size();
}

Size Slider::arrangeOverride(const Size & finalSize)
{
	return Size();
}
